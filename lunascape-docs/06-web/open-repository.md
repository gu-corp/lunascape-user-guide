# GitHub のリポジトリを開く

Web 版では、GitHub のリポジトリを指定して文書を開きます。公開リポジトリならログインは不要です。

## 画面から開く

1. <https://docs.lunascape.org/> を開きます。
2. ツールバーの [文書を開く]（フォルダのアイコン）を押します。
3. [リポジトリを直接指定] にリポジトリを入力し、[開く] を押します。
   GitHub にログインしているときは、[読めるリポジトリから選ぶ] で一覧から選ぶこともできます。

> **ヒント**
>
> - 隣にある GitHub のアイコンは、いま読んでいる文書を github.com で開きます。文書を開く操作ではありません。

## URL で開く

アドレスは、リポジトリと文書の位置をそのまま並べた形です。パスはリポジトリの中での位置なので、GitHub の URL と同じ並びになります。

```text
https://docs.lunascape.org/github/owner/repo/docs/01-product/vision.md
```

| 指定 | 書きかた |
|---|---|
| リポジトリのみ（既定ブランチ） | `/github/owner/repo` |
| リポジトリの中の文書 | `/github/owner/repo/docs/01-product/vision.md` |
| ブランチまたはタグを指定 | 末尾に `?ref=v1.2.0` を付けます |

ページを移動するとアドレスも変わります。ツールバーの [この文書を共有] を押すと、いま読んでいるページのリンクを渡せます。ブラウザの [戻る] [進む] も使えます。

以前の `?source=` の形も、これまでどおり開けます。開いたあとは新しい形に書き換わります。

```text
https://docs.lunascape.org/?source=github:owner/repo@main/docs#/01-product/vision.md
```

> **ご注意**
>
> - ログインしていない状態では、GitHub API の利用制限（1 時間あたり 60 回）があります。文書が多いリポジトリや繰り返しの閲覧では、[GitHub でログイン] してください。
> - `/` を含むブランチ名（`feature/xxx` など）は、上のアドレス形式の `?ref=` で指定できます。`?source=` の形では書けません。
> - 文書は閲覧者の GitHub 権限で読み込まれます。読み取り権限のない人には表示されません。

## ローカルフォルダの文書を開く

ツールバーの [文書を開く] を押し、一覧の下にある [ローカルフォルダの文書を開く] から、端末内のフォルダを選びます。ファイルはブラウザの中で処理され、外部へ送られることはありません。フォルダの選択に対応したブラウザ（Chrome、Edge など）で使えます。

## 関連項目

- [非公開リポジトリを閲覧する](private-repository.md)
- [Web 版で開けない・ログインできない](../07-troubleshooting/web.md)
